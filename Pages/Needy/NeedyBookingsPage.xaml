<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             x:Class="Foodshare.Pages.Needy.NeedyBookingsPage"
             Title="Мои бронирования">
    <CollectionView x:Name="List" Padding="12">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="12" Margin="0,8" BorderColor="#ddd" CornerRadius="12">
                    <VerticalStackLayout>
                        <Label Text="{Binding Title}" FontAttributes="Bold"/>
                        <Label Text="{Binding Status}" FontSize="12"/>
                        <Button Text="Позвонить ресторану" CommandParameter="{Binding RestPhone}" Clicked="Call_Clicked"/>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
